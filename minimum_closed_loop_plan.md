# Spatial Home 最小闭环实施方案

## 1. 目标

在一台不带 LiDAR 的 iPhone 上完成室内视频扫描，把数据交给一台 M2 Pro MacBook Pro，在本地自动生成带真实尺度的三维点云，并为下一阶段的房间尺寸估计留下稳定的数据接口。

这个版本的价值是展示一条完整、可测试、可评测、可持续改进的工程链路，不追求商业化，也暂不追求一次扫描就得到漂亮的完整户型模型。

## 2. 第一版的边界

### 必须完成

1. iPhone 原生 iOS 应用采集 RGB 图像。
2. 同步保存 ARKit 提供的相机位姿、相机内参、时间戳和跟踪状态。
3. 全程不要求用户点击墙角、墙面或天花板交点。
4. 扫描过程中自动提示用户放慢、回看或补扫某个方向。
5. 将一次扫描导出为版本化的 `ScanBundle`。
6. M2 Pro 运行 Depth Pro，对关键帧生成单目深度。
7. 使用深度、内参和位姿反投影，生成米制三维点云 `PLY`。
8. 产出一份机器可读的重建报告，记录耗时、帧数、跟踪质量和点云范围。

### 暂不完成

- 不使用 VGGT。
- 不使用在线生成式模型。
- 不依赖 7900 XT 工作站。
- 不做家具自动消除。
- 不承诺任意复杂房间的完整户型图。
- 不把苹果的 RoomPlan、场景深度或网格能力当成无 LiDAR 设备上的核心依赖。

## 3. 技术路线

```text
iPhone 摄像头
    │ RGB + ARKit 位姿/内参/跟踪状态
    ▼
ScanBundle
    │ 局域网上传或文件导出
    ▼
M2 Pro 重建服务
    ├─ 数据校验与关键帧筛选
    ├─ Depth Pro 单目深度
    ├─ 深度尺度与相机内参对齐
    ├─ 反投影到相机坐标系
    └─ 用 ARKit 位姿转换到世界坐标系
    ▼
depth.npy + depth_preview.png + pointcloud.ply + report.json
```

首版采用 M2 Pro 作为唯一受支持的重建端。这样可以优先使用 PyTorch MPS，环境比 SteamOS + AMD/ROCm 更容易固定和复现。7900 XT 后续可作为性能实验平台，但不进入第一版稳定性基线。

## 4. iOS 采集端设计

### 4.1 采集内容

每个关键帧保存：

- 未裁剪、未旋转的 JPEG 图像；
- ARKit 时间戳；
- `camera.transform`，明确保存为 4×4 行主序矩阵；
- `camera.intrinsics`，明确保存为 3×3 行主序矩阵；
- 相机标定分辨率和实际图像分辨率；
- ARKit tracking state 及受限原因；
- 图像方向和格式版本。

图像不能在保存时偷偷旋转或裁剪，否则内参会与像素坐标不一致。

### 4.2 自动关键帧策略

只有在以下条件满足时才保存关键帧：

- ARKit 跟踪状态正常；
- 与上一关键帧之间达到最小时间间隔；
- 相机发生了足够的平移或旋转；
- 图像不是明显模糊或曝光严重异常。

初版参数以可解释和稳定为主，不引入学习模型。建议一次扫描 15–30 秒，保存约 15–40 个关键帧。

### 4.3 无点击扫描引导

不要求用户标点。应用根据以下信号给出文字和颜色提示：

- ARKit tracking state：提示放慢或回看已经扫过的区域；
- 相机朝向覆盖分桶：提示向左、向右、向上或向下补扫；
- 平移/旋转速度：提示移动过快；
- 最近关键帧数量和覆盖度：提示继续或允许结束。

第一版的覆盖度是“相机观察方向覆盖度”，不是对墙体是否完整可见的语义判断。后续可以用重建后的墙面缺口反向生成更准确的补扫提示。

## 5. ScanBundle 数据契约

建议目录结构：

```text
<scan-id>.scanbundle/
├── manifest.json
└── frames/
    ├── 000001.jpg
    ├── 000002.jpg
    └── ...
```

`manifest.json` 至少包含：

```json
{
  "schema_version": "1.0",
  "scan_id": "UUID",
  "created_at": "ISO-8601",
  "device": {
    "model": "iPhone",
    "os_version": "iOS"
  },
  "coordinate_system": {
    "pose": "ARKit worldFromCamera",
    "matrix_layout": "row-major",
    "units": "meters"
  },
  "frames": [
    {
      "id": 1,
      "image": "frames/000001.jpg",
      "timestamp": 0.0,
      "image_width": 1920,
      "image_height": 1440,
      "intrinsics": [],
      "world_from_camera": [],
      "tracking_state": "normal"
    }
  ]
}
```

后端必须先验证版本、文件完整性、矩阵长度、分辨率和时间戳，再开始推理。未知 schema 版本应明确失败，不能静默猜测。

## 6. M2 Pro 重建端设计

### 6.1 首个真正闭环

先只选择一张质量最好的关键帧：

1. Depth Pro 推理得到米制深度图；
2. 按相机内参把有效像素反投影为三维点；
3. 将图像坐标系转换为 ARKit 相机坐标系；
4. 使用该帧位姿变换到 ARKit 世界坐标系；
5. 导出彩色 `PLY` 点云和深度预览图。

这一步不等于完整房间重建，但它能最快验证整条数据链路里最容易出错的部分：图像方向、内参、尺度、坐标系和矩阵布局。

### 6.2 第二个闭环：简单房间尺寸

在单帧闭环稳定后，再增加多帧处理：

1. 对多个关键帧分别预测深度并生成点云；
2. 使用 ARKit 位姿先验合并点云；
3. 进行体素降采样和离群点剔除；
4. 使用 RANSAC/区域生长拟合地面、天花板和墙面；
5. 施加曼哈顿世界假设：墙面主要互相平行或垂直；
6. 求墙面交线和房间包围轮廓；
7. 输出长、宽、高及每个数值的置信度。

这一阶段针对单个、近似矩形、墙面可见的简单房间优化。不要在算法尚未稳定时扩大到开放式户型、多房间或弧形墙。

### 6.3 后续的家具去除

家具去除放在房间尺寸闭环之后：

- 先从点云中找到大面积平面；
- 对墙面和地面上的遮挡区域做几何补全；
- 再考虑用图像分割模型识别可移动家具；
- 最终输出的是可编辑的墙、地面、天花板和平面边界，而不是生成式模型“想象”出的房间。

生成式模型以后可以负责视觉展示或纹理修复，但不作为尺寸真值来源。

## 7. 服务接口

第一版同时提供命令行入口和本地 HTTP 接口：

```text
POST /reconstruction/jobs       创建重建任务
GET  /reconstruction/jobs/{id}  查询状态和质量报告
GET  /reconstruction/jobs/{id}/artifacts/{name}  下载产物
```

内部任务状态固定为：`queued → validating → inferring → projecting → completed/failed`。每一步记录耗时和明确错误，便于展示“中台能力”和做稳定性评测。

## 8. 测试与评测

### 8.1 自动化测试

- Swift 单元测试：矩阵序列化、关键帧决策、manifest 编码。
- Python 单元测试：bundle 校验、内参缩放、坐标系转换、反投影。
- Golden Bundle 测试：固定一份小型扫描输入，验证输出文件、点数和包围盒不会意外变化。
- API 测试：任务状态、错误处理和产物下载。
- 端到端 smoke test：从示例 ScanBundle 生成 PLY，不要求每次运行完整模型推理。

### 8.2 实地评测

选择一个近似矩形的小房间，用卷尺测量长、宽、高作为真值。每种路径重复扫描至少 5 次，记录：

- 扫描成功率；
- 总处理时间；
- ARKit 正常跟踪帧比例；
- 点云有效点比例；
- 长、宽、高绝对误差和相对误差；
- 同一路径重复扫描的标准差。

第一阶段建议验收门槛：

- 5 次扫描至少 4 次可完整跑通；
- 不发生图像方向或坐标轴翻转；
- 单帧点云尺度和方向肉眼正确；
- 所有失败都有可定位的错误报告。

房间尺寸误差门槛要在收集首批真实数据后制定，避免没有数据就给出虚假的精度承诺。

## 9. 实施顺序

### M0：数据链路（优先）

- iOS 采集和无点击提示；
- ScanBundle 导出；
- Python bundle 校验器；
- 固定样例数据和契约测试。

### M1：单帧深度闭环

- M2 Pro 安装并锁定 Depth Pro 环境；
- 单帧深度、预览图和 PLY；
- 坐标系与尺度测试；
- 重建报告。

### M2：多帧与房间尺寸

- 多帧点云融合；
- 平面检测和曼哈顿约束；
- 长宽高输出及重复性评测；
- 根据缺失区域改进扫描提示。

### M3：家具去除实验

- 家具/结构分割；
- 墙地顶平面补全；
- 输出可编辑房间几何；
- 与原始点云并排展示结果。

## 10. 当前技术决策

| 问题 | 决策 |
|---|---|
| 是否要求用户点击墙角 | 否 |
| 是否依赖 LiDAR | 否 |
| 是否把 ARKit 当作深度模型 | 否，ARKit 负责位姿和内参 |
| 首版深度模型 | Depth Pro |
| 首版重建设备 | M2 Pro MacBook Pro |
| 是否使用 7900 XT | 暂不进入稳定基线 |
| 是否使用 VGGT | 首版不使用 |
| 是否使用在线生成模型 | 首版不使用 |
| 首个输出 | 单帧米制深度与彩色点云 |
| 第二个输出 | 简单矩形房间的长、宽、高和置信度 |

## 11. 开工时的第一个任务

先实现并冻结 `ScanBundle 1.0`：iOS 采集 3–5 个关键帧，Python 校验器能够读入，测试确认图像尺寸、内参和位姿一致。这个接口稳定后，再接 Depth Pro；否则模型输出异常时很难区分是模型问题还是采集数据问题。
